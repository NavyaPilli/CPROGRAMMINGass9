#include<stdio.h>
main()
{
	int num,sum=0,temp,rem;
	scanf("%d",&num);
	temp=num;
	while(num!=0)
	{
		rem=num%10;
		sum=sum*10+rem;
		num=num/10;
	}
	if(sum==temp)
	printf("%d is PALINDROME",temp);
	else
	printf("%d is not a PALINDROME",temp);
}
